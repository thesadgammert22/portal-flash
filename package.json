{
  "dependencies": {
    "babel-preset-react": "^6.24.1",
    "react": "^5.0.2",
    "react-dom": "^18.0.0",
    "react-scripts": "^18.0.0"
  },
  "main": "/index.html",
  "devDependencies": {
    "babel-cli": "^6.6.5"
  },
  "name": "2u18li",
  "description": null,
  "version": "0.0.1",
  "scripts": {
    "updatejs": "npm install -g n n latest",
    "babel-version": "babel --version",
    "start": "react-scripts --openssl-legacy-provider --legacy-peer-deps --force start",
    "build": "react-scripts build;",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
